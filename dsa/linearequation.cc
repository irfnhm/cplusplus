#include <iostream>
#include <string>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    string input;
    cin >> input;

	int a = input[0];
   	int b = input[1];
   	int c = input[2];

    cout << a << b << c;
}
